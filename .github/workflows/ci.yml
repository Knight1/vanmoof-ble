name: CI

on: [push, pull_request]

permissions:
  contents: read

jobs:
  test:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: ['ubuntu-latest', 'ubuntu-22.04-arm', 'macos-latest', 'windows-latest']
        python-version: ['3.8', '3.9', '3.10', '3.11', '3.12', '3.13', '3.14']

    steps:
    - uses: actions/checkout@v6

    - uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: pip install -r requirements.txt

    - name: Check syntax
      run: |
        python -m py_compile main.py
        python -m py_compile utils/credentials_utils.py
        python -m py_compile utils/crypto_utils.py
        python -m py_compile utils/protocol_utils.py
        python -m py_compile commands/lock.py
        python -m py_compile commands/alarm.py
        python -m py_compile commands/sound.py
        python -m py_compile commands/power.py
        python -m py_compile commands/ride.py
        python -m py_compile commands/lights.py
        python -m py_compile commands/info.py
        python -m py_compile commands/region.py
        python -m py_compile commands/telemetry.py

    - name: Run help
      run: python main.py --help